import java.io.ByteArrayInputStream
import java.nio.charset.Charset

import com.amazonaws.services.stepfunctions.builder.states.WaitFor
import org.scalatest.BeforeAndAfterAll
import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.should.Matchers

class LargeFileStreamingTest extends AnyFlatSpec with BeforeAndAfterAll with WaitFor with Matchers {

//  "reading large file " should "run" in {
//    val payload: Array[Byte] = "Large Payload".getBytes(Charset.defaultCharset())
//    val byteArrayInputStream = new ByteArrayInputStream(payload)
//
//    while (byteArrayInputStream.read() != -1 ) {
//
//    }

//  }

}
